"""Runtime primitives for the Kiwoom OpenAPI package.

Import concrete submodules (``kiwoom.core.errors``, ``kiwoom.core.secrets``,
...) or use the canonical package facade ``kiwoom`` (``get_auth``,
``get_client``, ``get_ws_client``, profiles, types).
"""
